# Database credentials
# TODO: Replace with your actual database credentials
database:
  host: localhost
  port: 5432
  name: airflow
  user: airflow
  password: airflow
  dialect: postgresql
  driver: psycopg2
